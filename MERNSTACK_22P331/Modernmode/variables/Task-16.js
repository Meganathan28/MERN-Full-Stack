const PIE=3.14;
var radius=0.5;
for(let i=0;i<1;i++){
    var areaofcircle=PIE*radius*radius;
}
alert(areaofcircle);
//const keyword is used to store the constant value which we cannot modified further.
//var is used anywhere in the block once we declare.
//let used only inside the scope,outside of the decalred block it won't perform.